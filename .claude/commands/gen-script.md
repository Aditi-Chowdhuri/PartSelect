Write or rewrite SCRIPT.md as a one-paragraph senior engineer technical summary of this project for a client audience. The paragraph should cover: the Claude tool-use agent loop, the 8 tools as thin data accessors, two-tier retrieval (JSON maps + FAISS), data sourcing from the Wayback Machine CDX API, SSE streaming from FastAPI to Next.js, and the deployment stack (Railway + Vercel). Write it as dense, confident prose — no bullet points, no headers, no markdown formatting inside the paragraph. Then commit and push.
