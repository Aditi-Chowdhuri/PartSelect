Audit the repo root for temporary, generated, or redundant files that should not be committed. Check for: leftover .docx files, one-off Python scripts (gen_*.py, convert_*.py), duplicate documentation files, and any .env files with real secrets. List what you find. For each file, state whether it should be deleted, gitignored, or kept. Do not delete anything without confirmation — present the list first.
