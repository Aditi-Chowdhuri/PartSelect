# The scraper requires no environment variables.
# All processing is done locally using FAISS and sentence-transformers.
# No API keys (Anthropic, Pinecone, OpenAI, etc.) are needed to run the pipeline.
